# api-crud-speed
데이터 CRUD 속도를 측정하는 API

# 사용 모듈
- django >= 3.x
- djangorestframewokr >= 3.x
```
# pip 사용시
$ pip install django djangorestframework
```

# 사용 방법
```
$ python ./api_crud_speed/manage.py migrate
$ python ./api_crud_speed/manage.py runserver {host}:{port}
```
